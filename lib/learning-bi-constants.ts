/** Shown in Learning BI UI — English technical note (avoid duplicating in "use server" actions file). */
export const LEARNING_BI_ATTEMPT_HISTORY_NOTE =
    "Per-lesson retry history is not stored (results are overwritten). Cohort curves by lesson order compare different lessons, not retries of the same lesson.";
